---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import CTA from '../../components/CTA.astro';
import StickyCTA from '../../components/StickyCTA.astro';

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Industries We Serve',
  description: 'AI-powered solutions tailored to specific industries',
  numberOfItems: 7,
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Aesthetics & Med Spas',
      url: 'https://monsoftsolutions.com/industries/aesthetics',
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: 'Local Businesses',
      url: 'https://monsoftsolutions.com/industries/local-business',
    },
    {
      '@type': 'ListItem',
      position: 3,
      name: 'Dental Practices',
      url: 'https://monsoftsolutions.com/industries/dental',
    },
    {
      '@type': 'ListItem',
      position: 4,
      name: 'HVAC & Contractors',
      url: 'https://monsoftsolutions.com/industries/contractors',
    },
    {
      '@type': 'ListItem',
      position: 5,
      name: 'Real Estate',
      url: 'https://monsoftsolutions.com/industries/real-estate',
    },
    {
      '@type': 'ListItem',
      position: 6,
      name: 'Restaurants',
      url: 'https://monsoftsolutions.com/industries/restaurants',
    },
    {
      '@type': 'ListItem',
      position: 7,
      name: 'Professional Services',
      url: 'https://monsoftsolutions.com/industries/professional-services',
    },
  ],
};

const industries = [
  {
    title: 'Aesthetics & Med Spas',
    description:
      'Fill your consultation calendar with AI-powered marketing. Automated SEO, patient communication, and conversion-optimized websites for plastic surgery clinics and med spas.',
    href: '/industries/aesthetics',
    icon: '💎',
    highlight: 'See how we helped Alluring Plastic Surgery increase traffic by 340%',
  },
  {
    title: 'Local Businesses',
    description:
      'Big business technology at small business prices. AI automation, local SEO, smart websites, and 24/7 AI assistants for service businesses in Southwest Florida.',
    href: '/industries/local-business',
    icon: '🏪',
    highlight: 'Contractors, salons, retail & more',
  },
  {
    title: 'Dental Practices',
    description:
      'Grow your patient base with automated appointment scheduling, recall systems, and reputation management designed specifically for dental practices.',
    href: '/industries/dental',
    icon: '🦷',
    highlight: 'New patient acquisition on autopilot',
  },
  {
    title: 'HVAC & Contractors',
    description:
      'Never miss a service call again. AI-powered lead capture, job scheduling, and follow-up automation built for home service professionals.',
    href: '/industries/contractors',
    icon: '🔧',
    highlight: 'Dominate local search in your service area',
  },
  {
    title: 'Real Estate',
    description:
      'Convert more leads into clients with AI assistants that qualify prospects, schedule showings, and nurture leads 24/7 while you close deals.',
    href: '/industries/real-estate',
    icon: '🏠',
    highlight: 'Automated lead nurturing that works',
  },
  {
    title: 'Restaurants',
    description:
      'Fill more seats with online reservations, review management, and local SEO that puts you at the top of "restaurants near me" searches.',
    href: '/industries/restaurants',
    icon: '🍽️',
    highlight: 'Increase covers and repeat customers',
  },
  {
    title: 'Professional Services',
    description:
      'Win more clients without cold calling. Lead capture websites, automated nurture sequences, and client portals for law firms, accountants, and consultants.',
    href: '/industries/professional-services',
    icon: '💼',
    highlight: 'Build your practice on referrals and reputation',
  },
];

const stats = [
  { value: '340%', label: 'Traffic Increase' },
  { value: '67%', label: 'More Consultations' },
  { value: '24/7', label: 'Lead Capture' },
  { value: '10+', label: 'Hours Saved Weekly' },
];
---

<Layout
  title="Industries We Serve | AI-Powered Solutions by Industry"
  description="Specialized AI automation and marketing solutions for aesthetics, dental, contractors, real estate, restaurants, and professional services. Industry-specific expertise that drives results."
  structuredData={structuredData}
>
  <Header />
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container-narrow">
        <span class="hero-label reveal">Industries We Serve</span>
        <h1 class="hero-title reveal reveal-delay-1">
          Solutions built for
          <span class="highlight">your industry</span>
        </h1>
        <p class="hero-subtitle reveal reveal-delay-2">
          We don't believe in one-size-fits-all. Our AI-powered solutions are tailored to the
          specific challenges and opportunities of your industry—because what works for a dental
          practice is different from what works for a restaurant.
        </p>
      </div>

      <!-- Floating orbs for visual interest -->
      <div class="hero-bg">
        <div class="orb orb-1"></div>
        <div class="orb orb-2"></div>
        <div class="orb orb-3"></div>
      </div>
    </section>

    <!-- Stats Bar -->
    <section class="stats-bar" id="stats-section">
      <div class="container-wide">
        <div class="stats-grid">
          {
            stats.map((stat, index) => (
              <div class={`stat reveal reveal-delay-${index + 1}`}>
                <span class="stat-value" data-value={stat.value}>
                  {stat.value}
                </span>
                <span class="stat-label">{stat.label}</span>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Industries Grid -->
    <section class="industries section-padding">
      <div class="container-wide">
        <div class="section-header reveal">
          <span class="section-label">Our Expertise</span>
          <h2>Deep industry knowledge, proven results</h2>
          <p>
            We go beyond generic solutions to deliver technology that speaks your language and
            solves your specific problems.
          </p>
        </div>

        <div class="industries-grid">
          {
            industries.map((industry, index) => (
              <a
                href={industry.href}
                class={`industry-card reveal reveal-delay-${(index % 3) + 1}`}
              >
                <div class="industry-icon">{industry.icon}</div>
                <div class="industry-content">
                  <h3 class="industry-title">{industry.title}</h3>
                  <p class="industry-description">{industry.description}</p>
                  <span class="industry-highlight">{industry.highlight}</span>
                  <span class="industry-link">
                    Learn more
                    <svg
                      width="16"
                      height="16"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                    >
                      <path
                        d="M5 12h14M12 5l7 7-7 7"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                      />
                    </svg>
                  </span>
                </div>
                <div class="industry-glow" />
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Why Industry Focus -->
    <section class="why-focus section-padding bg-dark">
      <div class="container-wide">
        <div class="why-grid">
          <div class="why-content reveal">
            <span class="section-label">Why Industry Focus</span>
            <h2>Specialists beat<br />generalists. Every time.</h2>
            <p>
              Generic marketing agencies treat every business the same. We've invested years
              understanding the nuances of specific industries—compliance requirements, customer
              journeys, competitive landscapes, and what actually drives results.
            </p>
          </div>
          <div class="why-features">
            <div class="why-feature reveal reveal-delay-1">
              <div class="why-feature-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path
                    d="M12 6.042A8.967 8.967 0 006 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 016 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 016-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0018 18a8.967 8.967 0 00-6 2.292m0-14.25v14.25"
                    stroke-linecap="round"
                    stroke-linejoin="round"></path>
                </svg>
              </div>
              <div>
                <h4>Industry-Specific Playbooks</h4>
                <p>
                  Proven strategies developed from working with dozens of businesses in your space.
                  We know what works.
                </p>
              </div>
            </div>
            <div class="why-feature reveal reveal-delay-2">
              <div class="why-feature-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path
                    d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"
                    stroke-linecap="round"
                    stroke-linejoin="round"></path>
                </svg>
              </div>
              <div>
                <h4>Compliance Built In</h4>
                <p>
                  Healthcare? Legal? We understand the regulations. Your marketing will be effective
                  AND compliant.
                </p>
              </div>
            </div>
            <div class="why-feature reveal reveal-delay-3">
              <div class="why-feature-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path
                    d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z"
                    stroke-linecap="round"
                    stroke-linejoin="round"></path>
                </svg>
              </div>
              <div>
                <h4>Faster Results</h4>
                <p>
                  No learning curve. We hit the ground running because we already understand your
                  business model.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Featured Case Study -->
    <section class="case-study section-padding">
      <div class="container-wide">
        <div class="section-header reveal">
          <span class="section-label">Featured Case Study</span>
          <h2>Real results for real businesses</h2>
        </div>
        <div class="case-content reveal reveal-delay-1">
          <div class="case-logo">
            <a href="https://alluringplasticsurgery.com" target="_blank" rel="noopener">
              alluringplasticsurgery.com
            </a>
          </div>
          <div class="case-results">
            <div class="case-result">
              <span class="result-value">340%</span>
              <span class="result-label">Traffic Increase</span>
            </div>
            <div class="case-result">
              <span class="result-value">67%</span>
              <span class="result-label">More Consultations</span>
            </div>
            <div class="case-result">
              <span class="result-value">24/7</span>
              <span class="result-label">Lead Capture</span>
            </div>
          </div>
          <blockquote class="case-quote">
            "We went from hoping patients would find us to having a predictable stream of qualified
            consultations."
            <cite>— Dr. Angelina Postoev, Alluring Plastic Surgery</cite>
          </blockquote>
          <a href="/industries/aesthetics" class="btn btn-primary"> See the Full Case Study </a>
        </div>
      </div>
    </section>

    <CTA />
  </main>
  <StickyCTA text="Ready to grow your business?" buttonText="Get Started" />
  <Footer />
</Layout>

<style>
  /* Hero */
  .hero {
    padding-top: 10rem;
    padding-bottom: 4rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: -1;
  }

  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
    animation: float 20s ease-in-out infinite;
  }

  .orb-1 {
    width: 500px;
    height: 500px;
    background: linear-gradient(135deg, #818cf8 0%, #c084fc 100%);
    top: -200px;
    right: -100px;
  }

  .orb-2 {
    width: 400px;
    height: 400px;
    background: linear-gradient(135deg, #22d3ee 0%, #34d399 100%);
    bottom: -150px;
    left: -100px;
    animation-delay: -7s;
  }

  .orb-3 {
    width: 300px;
    height: 300px;
    background: linear-gradient(135deg, #f472b6 0%, #fb923c 100%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation-delay: -14s;
    opacity: 0.2;
  }

  @keyframes float {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    25% {
      transform: translate(30px, -30px) scale(1.05);
    }
    50% {
      transform: translate(-20px, 20px) scale(0.95);
    }
    75% {
      transform: translate(20px, 10px) scale(1.02);
    }
  }

  .hero-label {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-gray-400);
    margin-bottom: 1rem;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: 1.5rem;
  }

  .hero-title .highlight {
    color: var(--color-gray-400);
  }

  .hero-subtitle {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--color-gray-500);
    max-width: 650px;
    margin-inline: auto;
  }

  /* Stats Bar */
  .stats-bar {
    background: var(--color-black);
    padding: 3rem 0;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    text-align: center;
  }

  .stat-value {
    display: block;
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--color-white);
    letter-spacing: -0.02em;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--color-gray-400);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Section Headers */
  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-gray-400);
    margin-bottom: 0.5rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 3.5vw, 2.75rem);
    font-weight: 500;
    margin-bottom: 1rem;
  }

  .section-header p {
    font-size: 1.0625rem;
    color: var(--color-gray-500);
    max-width: 600px;
    margin-inline: auto;
  }

  /* Industries Grid */
  .industries-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .industry-card {
    position: relative;
    display: flex;
    flex-direction: column;
    padding: 2rem;
    background: var(--color-white);
    border: 1px solid var(--color-gray-100);
    border-radius: 16px;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    overflow: hidden;
    text-decoration: none;
    color: inherit;
  }

  .industry-card:hover {
    border-color: var(--color-gray-200);
    transform: translateY(-8px);
    box-shadow:
      0 20px 40px -15px rgba(0, 0, 0, 0.1),
      0 0 0 1px rgba(0, 0, 0, 0.02);
  }

  .industry-glow {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
    background: radial-gradient(
      600px circle at var(--mouse-x, 50%) var(--mouse-y, 50%),
      rgba(99, 102, 241, 0.06),
      transparent 40%
    );
  }

  .industry-card:hover .industry-glow {
    opacity: 1;
  }

  .industry-icon {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
  }

  .industry-content {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .industry-title {
    font-size: 1.25rem;
    font-weight: 500;
    margin-bottom: 0.75rem;
    color: var(--color-black);
  }

  .industry-description {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--color-gray-500);
    margin-bottom: 1rem;
    flex: 1;
  }

  .industry-highlight {
    display: block;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-gray-600);
    margin-bottom: 1.5rem;
    padding: 0.5rem 0.75rem;
    background: var(--color-gray-50);
    border-radius: 6px;
    width: fit-content;
  }

  .industry-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-gray-600);
    transition: all 0.2s ease;
  }

  .industry-link svg {
    transition: transform 0.2s ease;
  }

  .industry-card:hover .industry-link {
    color: var(--color-black);
  }

  .industry-card:hover .industry-link svg {
    transform: translateX(4px);
  }

  /* Why Focus Section */
  .bg-dark {
    background: var(--color-black);
  }

  .bg-dark .section-label {
    color: var(--color-gray-500);
  }

  .bg-dark h2,
  .bg-dark h4 {
    color: var(--color-white);
  }

  .why-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 4rem;
    align-items: start;
  }

  .why-content h2 {
    font-size: clamp(2rem, 3.5vw, 2.75rem);
    font-weight: 500;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .why-content p {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--color-gray-400);
  }

  .why-features {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .why-feature {
    display: flex;
    gap: 1.25rem;
    align-items: flex-start;
  }

  .why-feature-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-gray-800);
    border-radius: 10px;
    flex-shrink: 0;
  }

  .why-feature-icon svg {
    width: 22px;
    height: 22px;
    color: var(--color-white);
  }

  .why-feature h4 {
    font-size: 1.0625rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .why-feature p {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--color-gray-400);
    margin: 0;
  }

  /* Case Study Section */
  .case-content {
    max-width: 700px;
    margin-inline: auto;
    text-align: center;
  }

  .case-logo {
    margin-bottom: 2rem;
  }

  .case-logo a {
    font-size: 1.125rem;
    font-weight: 500;
    color: var(--color-black);
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    background: var(--color-gray-50);
    border: 1px solid var(--color-gray-200);
    border-radius: 8px;
    display: inline-block;
    transition: all 0.2s ease;
  }

  .case-logo a:hover {
    border-color: var(--color-black);
  }

  .case-results {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-bottom: 2rem;
  }

  .case-result {
    text-align: center;
  }

  .result-value {
    display: block;
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--color-black);
    letter-spacing: -0.02em;
  }

  .result-label {
    font-size: 0.875rem;
    color: var(--color-gray-500);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .case-quote {
    font-size: 1.25rem;
    line-height: 1.7;
    color: var(--color-gray-600);
    font-style: italic;
    margin: 0 0 2rem 0;
    padding: 2rem;
    background: var(--color-gray-50);
    border-radius: 12px;
  }

  .case-quote cite {
    display: block;
    font-size: 0.9375rem;
    color: var(--color-gray-500);
    font-style: normal;
    margin-top: 1rem;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .industries-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .why-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .case-results {
      gap: 2rem;
    }
  }

  @media (max-width: 768px) {
    .industries-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 8rem;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    .stat-value {
      font-size: 2rem;
    }

    .case-results {
      flex-direction: column;
      gap: 1.5rem;
    }

    .result-value {
      font-size: 2rem;
    }

    .case-quote {
      font-size: 1.0625rem;
      padding: 1.5rem;
    }
  }
</style>

<script>
  // Add mouse position tracking for glow effect
  document.querySelectorAll('.industry-card').forEach((card) => {
    (card as HTMLElement).addEventListener('mousemove', (e) => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      (card as HTMLElement).style.setProperty('--mouse-x', `${x}px`);
      (card as HTMLElement).style.setProperty('--mouse-y', `${y}px`);
    });
  });

  // Animated stats counter
  function animateValue(
    element: HTMLElement,
    start: number,
    end: number,
    duration: number,
    suffix: string,
    prefix: string
  ) {
    const range = end - start;
    const startTime = performance.now();
    function update(currentTime: number) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const easeOut = 1 - Math.pow(1 - progress, 3);
      const current = Math.floor(start + range * easeOut);
      element.textContent = prefix + current + suffix;
      if (progress < 1) requestAnimationFrame(update);
      else element.textContent = prefix + end + suffix;
    }
    requestAnimationFrame(update);
  }

  function parseStatValue(value: string) {
    const match = value.match(/^([<>$]?)(\d+(?:\.\d+)?)(.*?)$/);
    return match
      ? { prefix: match[1] || '', number: parseFloat(match[2]), suffix: match[3] || '' }
      : { prefix: '', number: 0, suffix: value };
  }

  const statsSection = document.getElementById('stats-section');
  if (statsSection) {
    const statsObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.querySelectorAll('.stat-value').forEach((el, index) => {
              const element = el as HTMLElement;
              const parsed = parseStatValue(element.dataset.value || '');
              if (parsed.number > 0) {
                element.textContent = parsed.prefix + '0' + parsed.suffix;
                setTimeout(
                  () => animateValue(element, 0, parsed.number, 1500, parsed.suffix, parsed.prefix),
                  index * 100
                );
              }
            });
            statsObserver.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.3 }
    );
    statsObserver.observe(statsSection);
  }
</script>
